{% extends 'base.html.twig' %}

{% block body %}
    {% if isOk %}
        <h1>vos données ont bien été modifié</h1>
    {% else %}
        {{ form_start(vehicleForm) }}
        {{ form_rest(vehicleForm) }}
        <button type="submit">Modifier</button>
        {{ form_end(vehicleForm) }}
    {% endif %}
{% endblock %}